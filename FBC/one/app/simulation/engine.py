# from app.core.managers.vehicle import VehicleManager


# class SimulationEngine:
#     def __init__(self):
#         self.vehicle_manager = VehicleManager()
#         self.traffic_manager = TrafficManager()
#         self.trip_manager = TripManager()
        
#     def update(self, delta_time: float):
#         self.traffic_manager.update(delta_time)
#         self.vehicle_manager.update(delta_time)
#         self.trip_manager.update(delta_time)